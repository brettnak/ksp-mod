---

name: module_manager

human_name: Module Manager

# If the developer uses a different versioning scheme, use this to
# indicate the developer's listed version.
original_version: 2.1.5

version:
  major: 2
  minor: 1
  patch: 5

url: https://ksp.sarbian.com/jenkins/job/ModuleManager/lastBuild/artifact/jenkins-ModuleManager-36/ModuleManager-2.1.5.zip

# Only type supported
archive_type: zip

# All `src:` paths are relative the root of the extracted zip.
# All `dest:` paths are relative the KSP root
# These can be files OR directories.  Make sure not to include dependencies here.
# Only include dependencies to other plugins in the dependencies section
files:
  -
    src:  ModuleManager.2.1.5.dll
    dest: GameData/ModuleManager.2.1.5.dll

# Not Implemented
after_install:
  - null

# Not Implemented
after_uninstall:
  - null

# Not Implemented
dependencies: []
